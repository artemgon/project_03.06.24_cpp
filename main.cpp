#include "functions.h"

int main()
{
	//expFunction(base, power, result)
	//When you call a function, the "result" parameter should equal to "base" parameter
	int result = expFunction(2, 3, 2);
	cout << result;
	return 0;
}
